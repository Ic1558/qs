# DIFF

- expanded `docs/ACCEPTANCE.md` from a scaffold-level gate note into a discipline-gated acceptance policy with ST / AR / MEP / Universal proof rules
- rewrote `docs/ARCHITECTURE.md` to document the multi-discipline aggregation architecture and bridge/orchestration path
- added `docs/AR_MEMBER_MODEL.md` to define the architectural domain objects and deduction model before broader AR capability claims
- explicitly excluded `pyproject.toml`, runtime code, tests, generated files, and packaging metadata from this PR
